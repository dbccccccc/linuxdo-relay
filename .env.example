# ===========================================
# Docker Compose 配置
# ===========================================
# PostgreSQL 配置
POSTGRES_USER=linuxdo_relay
POSTGRES_PASSWORD=linuxdo_relay
POSTGRES_DB=linuxdo_relay

# 应用端口
APP_PORT=8080

# ===========================================
# 应用配置
# ===========================================
# HTTP 服务配置
APP_HTTP_LISTEN=:8080

# PostgreSQL 数据库配置
# - Docker Compose 默认通过 Setup 向导填写，不需要在此指定
# - 如需直接运行二进制或跳过 Setup，可取消注释并填入本地/外部 DB 的 DSN
# APP_PG_DSN=postgres://user:password@localhost:5432/linuxdo_relay?sslmode=disable

# Redis 配置
# 默认本地开发指向 localhost；Compose 会在运行时写入 runtime config
APP_REDIS_ADDR=localhost:6379
APP_REDIS_PASSWORD=

# JWT 密钥（必填，至少 32 字符）
APP_JWT_SECRET=your-very-secure-secret-key-change-me-at-least-32-chars

# 积分系统配置
APP_SIGNUP_CREDITS=100
APP_DEFAULT_MODEL_CREDIT_COST=1

# LinuxDo OAuth 配置（必填）
# 在 https://connect.linux.do/ 创建应用获取
APP_LINUXDO_CLIENT_ID=your-client-id
APP_LINUXDO_CLIENT_SECRET=your-client-secret
APP_LINUXDO_REDIRECT_URL=http://localhost:8080/auth/linuxdo/callback

# LinuxDo OAuth 端点（可选，使用默认值）
# APP_LINUXDO_AUTH_URL=https://connect.linux.do/oauth2/authorize
# APP_LINUXDO_TOKEN_URL=https://connect.linux.do/oauth2/token
# APP_LINUXDO_USERINFO_URL=https://connect.linux.do/api/user
